# Arbeitsbericht am 12.11.2025

---

- Thema: Übungen stdin/stdout
- Name: Lukas EDER
- Klasse: 2AHITS Gruppe 1
- Fach: ITSI Übungen

---

# Übungen stdin/stdout

---

## Übung (Kopie mit cat)

### Aufgabenstellung:

Erstelle mit Hilfe von “cat” eine Kopie der Datei test.txt in der neuen Datei test2.txt. test2.txt soll den gleichen Inhalt enthalten wie test.txt. Prüfe ob test2.txt wirklich den gewünschten Inhalt hat.

### Aufgabe:

```sh
touch test.txt                #Datei erstellen
echo KonradLaimer >test.txt   #Text in Datei einfügen

cat test.txt >test2.txt       #Datei kopieren
cat test2.txt                 #Text in der neuen Datei ausgeben
```
Ausgabe:
```sh
KonradLaimer
```

---

## Übung (dirlist in File)

### Aufgabenstellung:

Schreibe den, mittels ls ermittelten, Inhalt des Verzeichnisses /etc in eine Textdatei mit dem Namen etcdir.txt. Inhalt des Verzeichnisses = die Namen der enthaltenen Files und Unterverzeichnisse.

### Aufgabe:

```sh
ls /etc > etcdir.txt

cat etcdir.txt
```
Ausgabe:
```sh
alternatives
apt
bash.bashrc
bash_completion.d
bindresvport.blacklist
ca-certificates
ca-certificates.conf
cloud
...
```

---

## Übung (os-release)

### Aufgabenstellung:
Es gibt eine Datei /etc/os-release.

- Betrachte deren Inhalt mit cat, welche Informationen kannst du daraus ableiten. Würde diese Information einem Hacker helfen der sich Zugriff auf das System verschafft hat? Wenn ja, wie?
- Wie wird die Datei mit ls -l angezeigt? Was bedeutet diese Anzeige?

### Aufabe:

**Inhalt mit `cat` betrachten:**
```sh
cat /etc/os-release

```
Ausgabe:

```sh
PRETTY_NAME="Ubuntu 24.04.2 LTS"
NAME="Ubuntu"
VERSION_ID="24.04"
VERSION="24.04.2 LTS (Noble Numbat)"
VERSION_CODENAME=noble
....
```

Ja es würde einem Hacker helfen weil er viele Informationen dadurch bekommt wie Betriebssystem und Version und den Ubuntu Codename wodurch man auch die Version ablesen kann.


## Übung (Textdatei erstellen)

Datei erstellen:
```sh
echo made_by_echoing.txt
```

Inhalt befüllen:
```sh
echo ===================== >> made_by_echoing.txt 
echo =    HTL BRAUNAU    = >> made_by_echoing.txt
echo = 2AHITS Gruppe 1   = >> made_by_echoing.txt
echo = 0 Schülerinnen    = >> made_by_echoing.txt
echo = 13 Schüler        = >> made_by_echoing.txt
echo ===================== >> made_by_echoing.txt
```

Dokument Inhalt:

```
=====================
= HTL BRAUNAU       =
= 2AHITS Gruppe 1   =
= 0 Schülerinnen    =
= 13 Schüler        =
=====================
```